# 환경변수 (절대 커밋 금지)
.env
.env.*
.env.local
.env*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
*.whl

# 가상환경
venv/
.venv/
env/
.env/

# 테스트 / 커버리지
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 로그 (로컬 전용)
observability_log.jsonl

# Docker
*.dockerignore

# 임시 작업 스크립트
scratch/

# Node.js (webapp)
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# 세션 체크포인트 (로컬 전용)
.claude/checkpoints/

# 이식 테스트 산출물 (로컬 전용)
test_transplant_target/

# 피지수 학습용 감시 폴더 (방부장 업로드 엑셀·로컬 전용)
watch/
learn_log.jsonl

# 코드베이스 증분 학습 상태 (로컬 전용)
learn_state.json
.learn_state.json

# 임시 메시지·스크립트 (회고/통신/검증용 — 단군 청에 따라 watcher.py 등 보존)
.tmp_*

# 통신·산출물은 root에 흩어지면 무시. 정주는 physis_memory/raw/ (2026-05-10 정리)
/_*.json
/_*.txt
/output_*.json
/d:/

# 벡터 DB 테스트 디렉토리
library/vector_db_test/

# ── physis_memory — 바이너리/대용량 제외, 마크다운은 포함 ─────────────
# ChromaDB 임베딩 벡터 (56MB 바이너리 — 재생성 가능)
physis_memory/.chromadb/

# DuckDB 바이너리 DB + WAL (재생성 가능)
physis_memory/physis_brain.duckdb
physis_memory/physis_brain.duckdb.wal
physis_memory/physis_brain.duckdb.tmp

# library/vector_db (24MB 바이너리 — 이미 tracked이지만 불필요)
library/vector_db/

# ── physis-metabolism 상태 파일 (로컬 전용) ──────────────────────────
physis-metabolism/.obsidian_sync_state.json
physis-metabolism/.outbox_to_obsidian_state.json
physis-metabolism/.dangun_last_polled
physis-metabolism/.lint_last_run

# metabolism 로그 (로컬 전용, 대용량)
physis-metabolism/logs/
physis-metabolism/dangun_escalation/
physis-metabolism/stimuli/
physis-metabolism/outbox/
physis-metabolism/patches/

# Obsidian 개인 설정 (선택적 — workspace는 기기마다 다름)
physis_memory/.obsidian/workspace.json
physis_memory/.obsidian/workspace-mobile.json

# 야간학습 자동 갱신 파일 (단군 권고 b — baseline은 commit 91e670e에 박혀 있음)
.titans_state.json
brain.md
judgment.md
